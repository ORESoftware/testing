# Known gaps before the first reviewed release

1. Create the authoritative `ORESoftware/mcp-rust-libs` repository through the reviewed repository-administration workflow; the target still returns 404 and is blocked by Linear `DEN-319`.
2. Promote the semantic merge from draft carrier `ORESoftware/testing#2` into the authoritative repository rather than merging the subtree into the legacy `testing` default branch.
3. Run the expanded Rust, TypeScript, Dart, and Gleam CI and commit dependency lockfiles generated by those reviewed jobs. The preserved six-crate Rust lockfile is evidence only and is intentionally not masquerading as the eight-crate active lock.
4. Resolve any native compiler findings without weakening the validated Rust security behavior or the cross-language fixture contract.
5. Extend `ore-mcp-codegen` from its deterministic registered-template bootstrap to general emission over the supported portable JSON Schema profile.
6. Add strict raw-JSON closed-object decoding for Gleam so all 38 fixtures execute through the native Gleam decoder rather than only typed-value validation.
7. Add before/after tool-schema and server-info snapshots from the pilot repositories.
8. Add an OpenTelemetry 0.27 compatibility adapter or complete a reviewed fleet upgrade before migrating older servers.
9. Produce the Zed lockfile, `zed release plan`, preflight, deterministic double-pack, clean-room install evidence, SBOM/provenance, and coordinated release-set metadata.
