{erl_opts, [debug_info, warn_unused_vars]}.

{deps, [
    {jsx, "3.1.0"}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info, warn_unused_vars, {d, 'TEST'}]}
    ]}
]}.

{eunit_opts, [verbose]}.
