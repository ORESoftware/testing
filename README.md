# Meta-Agent Control Plane publication bootstrap

This branch reconstructs the checksum-verified `DEN-1057` source tree, then replaces this temporary bootstrap commit with normal browsable source files. The target `meta-agents-demo/meta-agent-control-plane.rs` repository does not yet exist through the connected repository-creation surface.
